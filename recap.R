# Crate a object named "bird_weight" having list of weights - 50, 50, 55, 60, 63, 65, 66, 72, 75
# Create a matrix having 1 to 24 having four rows and six columns
# Create a dataframe having one row all numeric and another all characters a to z
# Retrieve only upto 4th row of dataframe